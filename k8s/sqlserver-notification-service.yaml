apiVersion: v1
kind: Service
metadata:
  name: sqlserver-notification
spec:
  selector:
    app: sqlserver-notification
  ports:
    - protocol: TCP
      port: 1433
      targetPort: 1433
  type: ClusterIP
