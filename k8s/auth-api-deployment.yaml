apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-api
  template:
    metadata:
      labels:
        app: auth-api
    spec:
      initContainers:
        - name: wait-for-sql
          image: busybox:1.36
          command:
            - sh
            - -c
            - until nc -z sqlserver-auth 1433; do echo waiting for sqlserver-auth; sleep 5; done
        - name: wait-for-rabbitmq
          image: busybox:1.36
          command:
            - sh
            - -c
            - until nc -z rabbitmq 5672; do echo waiting for rabbitmq; sleep 5; done
      containers:
        - name: auth-api
          image: fiapgames/auth-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
          env:
            - name: ASPNETCORE_ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: auth-api-config
                  key: ASPNETCORE_ENVIRONMENT
            - name: ASPNETCORE_URLS
              valueFrom:
                configMapKeyRef:
                  name: auth-api-config
                  key: ASPNETCORE_URLS
            - name: ConnectionStrings__FIAPGamesConnection
              valueFrom:
                secretKeyRef:
                  name: auth-api-secret
                  key: ConnectionStrings__FIAPGamesConnection
            - name: Jwt__Key
              valueFrom:
                secretKeyRef:
                  name: auth-api-secret
                  key: Jwt__Key
            - name: Jwt__Issuer
              valueFrom:
                configMapKeyRef:
                  name: auth-api-config
                  key: Jwt__Issuer
            - name: Jwt__Audience
              valueFrom:
                configMapKeyRef:
                  name: auth-api-config
                  key: Jwt__Audience
            - name: RabbitMq__HostName
              valueFrom:
                configMapKeyRef:
                  name: auth-api-config
                  key: RabbitMq__HostName
            - name: RabbitMq__Port
              valueFrom:
                configMapKeyRef:
                  name: auth-api-config
                  key: RabbitMq__Port
            - name: RabbitMq__UserName
              valueFrom:
                secretKeyRef:
                  name: auth-api-secret
                  key: RabbitMq__UserName
            - name: RabbitMq__Password
              valueFrom:
                secretKeyRef:
                  name: auth-api-secret
                  key: RabbitMq__Password
          readinessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 20
