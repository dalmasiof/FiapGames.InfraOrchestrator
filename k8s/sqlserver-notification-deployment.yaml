apiVersion: apps/v1
kind: Deployment
metadata:
  name: sqlserver-notification
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sqlserver-notification
  template:
    metadata:
      labels:
        app: sqlserver-notification
    spec:
      containers:
        - name: sqlserver-notification
          image: mcr.microsoft.com/mssql/server:2022-latest
          ports:
            - containerPort: 1433
          env:
            - name: ACCEPT_EULA
              value: "Y"
            - name: MSSQL_SA_PASSWORD
              value: "YourStrong@Passw0rd"
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P 'YourStrong@Passw0rd' -C -Q 'SELECT 1'"
            initialDelaySeconds: 30
            periodSeconds: 10
          volumeMounts:
            - mountPath: /var/opt/mssql
              name: mssql-notification-data
      volumes:
        - name: mssql-notification-data
          emptyDir: {}
